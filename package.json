{
  "name": "json-server-deployment",
  "version": "1.0.0",
  "description": "Simple JSON Server API for deployment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
